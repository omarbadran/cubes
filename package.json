{
  "name": "cubes",
  "packageManager": "yarn@3.2.2",
  "workspaces": [
    "packages/apps/*",
    "packages/modules/*"
  ],
  "scripts": {
    "format": "prettier --check packages/**/*.ts --write"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  }
}
